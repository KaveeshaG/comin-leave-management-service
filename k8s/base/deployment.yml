apiVersion: apps/v1
kind: Deployment
metadata:
  name: comin-leave-management-service
  namespace: comin-dev
  labels:
    app: comin-leave-management-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: comin-leave-management-service
  template:
    metadata:
      labels:
        app: comin-leave-management-service
    spec:
      containers:
      - name: comin-leave-management-service
        image: ghcr.io/kaveeshag/comin-leave-management-service:77c853faa8ffa298eebc54b047bd8ef0e3e92112
        imagePullPolicy: Always
        ports:
        - containerPort: 8083
        env:
        - name: GO_ENV
          value: "development"
        resources:
          limits:
            cpu: "0.5"
            memory: "512Mi"
          requests:
            cpu: "0.2"
            memory: "256Mi"