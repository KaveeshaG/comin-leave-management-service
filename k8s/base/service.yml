apiVersion: v1
kind: Service
metadata:
  name: comin-leave-management-service
  namespace: comin-dev
spec:
  selector:
    app: comin-leave-management-service
  ports:
  - port: 80
    targetPort: 8083
  type: ClusterIP